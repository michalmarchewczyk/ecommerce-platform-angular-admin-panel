import { FormatCountryPipe } from './format-country.pipe';

describe('FormatCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
